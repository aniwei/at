{
  "name": "at",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "prepare": "husky install",
    "dev": "pnpm --filter=@at/* run dev",
    "build": "pnpm --filter=@deco/* run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "lint-staged": {
    "src/**/*.{js,jsx,ts,tsx}": ["eslint --fix --quiet"]
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@typescript-eslint/parser": "^5.50.0",
    "commitlint": "^17.4.2",
    "eslint": "^8.33.0",
    "eslint-config-o2team": "^0.1.7",
    "husky": "^8.0.3",
    "lint-staged": "^13.1.0"
  }
}
