import { Document } from '../lib/document'
import { BlendMode, ElementKind, ElementPhaseKind, ElementStateKind, PaintKind, PaintStateKind } from '../lib/element'
import { documentWrite } from '../lib/write'

const document = Document.create()
document.start().then(() => {
  

})


